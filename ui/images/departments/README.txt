Drop PNG icons for each department in this folder.

Naming convention (case insensitive):
  LSPD -> lspd.png
  SAHP -> sahp.png

After adding or replacing files, run `refresh` + `ensure PoliceDutyBlipSystem` or `restart PoliceDutyBlipSystem` on the server so the UI picks up the new icons.
